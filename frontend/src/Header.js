import React from 'react';

function Header() {
  return (
    <header>
      <h1>Scheduling AI</h1>
      {/* We can add buttons or other elements later */}
    </header>
  );
}

export default Header;